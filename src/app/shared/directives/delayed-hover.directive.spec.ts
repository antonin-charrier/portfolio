import { DelayedHoverDirective } from './delayed-hover.directive';

describe('DelayedHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayedHoverDirective();
    expect(directive).toBeTruthy();
  });
});
