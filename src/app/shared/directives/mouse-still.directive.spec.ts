import { MouseStillDirective } from './mouse-still.directive';

describe('MouseStillDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseStillDirective();
    expect(directive).toBeTruthy();
  });
});
